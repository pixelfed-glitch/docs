import{_ as e,c as o,o as i,V as t}from"./chunks/framework.wUJtDaaB.js";const f=JSON.parse('{"title":"Pixelfed + Docker FAQ","description":"","frontmatter":{},"headers":[],"relativePath":"running-pixelfed/docker/faq.md","filePath":"running-pixelfed/docker/faq.md"}'),r={name:"running-pixelfed/docker/faq.md"},a=t('<h1 id="pixelfed-docker-faq" tabindex="-1">Pixelfed + Docker FAQ <a class="header-anchor" href="#pixelfed-docker-faq" aria-label="Permalink to &quot;Pixelfed + Docker FAQ&quot;">​</a></h1><div class="tip custom-block"><p class="custom-block-title">If anything is confusing, unclear, missing, or maybe even wrong on this page, then <em>please</em> let us know <a href="https://github.com/pixelfed-glitch/pixelfed/issues/new" target="_blank" rel="noreferrer">by submitting a bug report</a> ❤️</p></div><h2 id="how-do-i-use-my-own-proxy-server" tabindex="-1">How do I use my own Proxy server? <a class="header-anchor" href="#how-do-i-use-my-own-proxy-server" aria-label="Permalink to &quot;How do I use my own Proxy server?&quot;">​</a></h2><p>No problem! All you have to do is:</p><ol><li>Change the <code>DOCKER_PROXY_PROFILE</code> key/value pair in your <code>.env</code> file to <code>&quot;disabled&quot;</code>. <ul><li>This disables the <code>proxy</code> <em>and</em> <code>proxy-acme</code> services in <code>docker-compose.yml</code>.</li><li>The setting is near the bottom of the file.</li></ul></li><li>Point your proxy upstream to the exposed <code>web</code> port (<strong>Default</strong>: <code>8080</code>). <ul><li>The port is controlled by the <code>DOCKER_WEB_PORT_EXTERNAL_HTTP</code> key in <code>.env</code>.</li><li>The setting is near the bottom of the file.</li></ul></li><li>Run <code>docker compose up -d --remove-orphans</code> to apply the configuration</li></ol><h2 id="how-do-i-use-my-own-ssl-certificate" tabindex="-1">How do I use my own SSL certificate? <a class="header-anchor" href="#how-do-i-use-my-own-ssl-certificate" aria-label="Permalink to &quot;How do I use my own SSL certificate?&quot;">​</a></h2><p>No problem! All you have to do is:</p><ol><li>Change the <code>DOCKER_PROXY_ACME_PROFILE</code> key/value pair in your <code>.env</code> file to <code>&quot;disabled&quot;</code>. <ul><li>This disabled the <code>proxy-acme</code> service in <code>docker-compose.yml</code>.</li><li>It does <em>not</em> disable the <code>proxy</code> service.</li></ul></li><li>Put your certificates in <code>${DOCKER_ALL_HOST_CONFIG_ROOT_PATH}/proxy/certs</code> (e.g. <code>./docker-compose/config/proxy/certs</code>) <ul><li>You may need to create this folder manually if it does not exist.</li><li>The following files are expected to exist in the directory for the proxy to detect and use them automatically (this is the same directory and file names as LetsEncrypt uses) <ol><li><code>${APP_DOMAIN}.cert.pem</code></li><li><code>${APP_DOMAIN}.chain.pem</code></li><li><code>${APP_DOMAIN}.fullchain.pem</code></li><li><code>${APP_DOMAIN}.key.pem</code></li></ol></li><li>See the <a href="https://github.com/nginx-proxy/nginx-proxy/blob/main/nginx.tmpl#L659-L670" target="_blank" rel="noreferrer"><code>nginx-proxy</code> configuration file for name patterns</a></li></ul></li><li>Run <code>docker compose up -d --remove-orphans</code> to apply the configuration</li></ol><h2 id="how-do-i-change-the-container-name-prefix" tabindex="-1">How do I change the container name prefix? <a class="header-anchor" href="#how-do-i-change-the-container-name-prefix" aria-label="Permalink to &quot;How do I change the container name prefix?&quot;">​</a></h2><p>Change the <code>DOCKER_ALL_CONTAINER_NAME_PREFIX</code> key/value pair in your <code>.env</code> file.</p>',10),c=[a];function l(d,n,s,h,p,u){return i(),o("div",null,c)}const _=e(r,[["render",l]]);export{f as __pageData,_ as default};
